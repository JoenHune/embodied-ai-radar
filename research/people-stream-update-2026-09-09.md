# 人物列表连续加载 · 2026-09-09

## 范围

仅调整 `/organizations/people/`，保留其他页面的列表方式、研究数据、筛选语义和既有照片策略。未提交、推送或发布线上。

- 首批展示 12 位，底部进入视口附近时每次追加 12 位；既有卡片不移除。
- 全部展示后给出结束提示；保留“继续加载”作为键盘及不支持观察器时的兜底入口。
- 人物详情打开期间暂停追加，关闭及同筛选条件历史导航保留已加载数量。
- 仅在姓名、方向、视角或排序实际改变时重置；重复选择同一视角不会折叠列表。
- 追加后重新观察底部，覆盖大视口下标记持续可见的情况；卸载时清理观察器。

## 一并修复的导航问题

VitePress 的捕获阶段链接监听早于 Vue 的点击阻止，人物查询链接会重载初始页面组件，将已加载列表重置。5 个人物链接入口现使用局部 `vp-raw` 标记；人物页用可清理的同路径加载钩子恢复查询状态，避免 history 导航重挂载，跨页导航继续使用原路由。

## 验证

- 自动测试：734 项 Python、118 项 Node 全部通过；类型检查、VitePress 构建、Pagefind 构建、站点与配图审计通过。
- 正式预览实测向下滚动：12 → 24 → 32 位，原始人物仍保留，结束提示正确；无上一页／下一页控件。
- 最后一批 Yilun Du 详情打开时列表仍为 32，关闭后仍为 32；打开和关闭时 scrollY 同为 5516.5。
- 浏览器返回再次打开详情时仍为 32，并保持同一滚动位置；再次返回可关闭详情。
- 重复点击当前观察视角后仍为 32；世界模型筛选展示 6 位；空搜索结果没有底部观察标记；键盘清空搜索恢复首批 12 位。
- 研究组跨页入口可用，返回人物页可恢复查询状态。
- 无浏览器调试注入或临时视口覆盖遗留；本地预览继续运行。

测试日志：`logs/people-stream-tests.log`。实现：`PeopleRadar.vue`、`people-stream.mjs` 及对应类型与回归测试。
