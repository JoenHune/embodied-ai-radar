# 型号字典的历史内容快照

文件名是整个字典按 `catalog_store.fingerprint` 计算的 SHA-256。当前字典在 `../hardware-dictionary.json`；已提交过的更早版本也可以从 Git 历史恢复。

本目录保存尚未成为独立发布版本、但已经用于持久化扫描记录的字典内容，避免只留下不可复算的旧哈希。`3cac93142575f51b605320774cf3cf33249dab6918325c67879e0ad47454dff7.json` 是加入 ARX-5 前的273条字典；版本标签仍为3，精确内容以哈希区分。正式版本3当前为274条。

这些仅是发现字典，不是已核验设备使用关系。历史扫描仍保留；当前统计只采用与当前字典及来源内容哈希均匹配的记录。
